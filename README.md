### To run the project
- `docker compose up --watch`

### To stop the server
- `docker compose down` or docker compse down -v` (to remove volumes)

# Alternatives to start the server
- `docker compose run app /bin/bash` or `docker compose run app python`